var nota = 8;


var status = nota > 7 ? "Aprovado" : "Reprovado";

console.log(status)

// ? => IF
// : => ELSE
// A variavel status recebera valor Aprovado se nota for maior que 7
// Se não vai receber o valor Reprovado.